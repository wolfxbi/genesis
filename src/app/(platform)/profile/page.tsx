import { PageCard } from "@/components/page-card";

export default function Page() {
  return <PageCard title="Profile" eyebrow="Operator Identity" description="Manage personal workspace details, preferences, and command-center presence." stats={['Role overview', 'Access posture', 'Personalization']} />;
}
