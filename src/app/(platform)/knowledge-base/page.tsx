import { PageCard } from "@/components/page-card";

export default function Page() {
  return <PageCard title="Knowledge Base" eyebrow="Trusted Context" description="Curate the source-of-truth layer that will ground agents, chat, and BI narratives." stats={['Topic maps', 'Source quality', 'Retrieval ready']} />;
}
