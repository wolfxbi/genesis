import { PageCard } from "@/components/page-card";

export default function Page() {
  return <PageCard title="Documents" eyebrow="Enterprise Content" description="Upload, organize, and prepare business documents for governed AI review and extraction." stats={['Document vault', 'Classification lanes', 'Review queue']} />;
}
