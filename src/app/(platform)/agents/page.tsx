import { PageCard } from "@/components/page-card";

export default function Page() {
  return <PageCard title="AI Agents" eyebrow="Agent Mesh" description="Design specialist AI workers for research, operations, sales, support, and analytics." stats={['Agent registry', 'Role playbooks', 'Human approval']} />;
}
