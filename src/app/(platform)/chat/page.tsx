import { PageCard } from "@/components/page-card";

export default function Page() {
  return <PageCard title="AI Chat" eyebrow="Conversational Intelligence" description="Coordinate executive prompts, operational answers, and multimodal AI workflows from a focused chat surface." stats={['Prompt workspace', 'Conversation memory', 'Secure handoffs']} />;
}
