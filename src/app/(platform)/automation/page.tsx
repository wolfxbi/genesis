import { PageCard } from "@/components/page-card";

export default function Page() {
  return <PageCard title="Automation" eyebrow="Workflow Engine" description="Orchestrate repeatable business processes with AI-assisted routing and approvals." stats={['Trigger library', 'Run history', 'Approval gates']} />;
}
